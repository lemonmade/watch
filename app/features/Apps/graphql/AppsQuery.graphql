query Apps {
  apps {
    id
    name
    isInstalled
    extensions {
      ... on ClipsExtension {
        __typename
        id
        name
        isInstalled
        latestVersion {
          extends {
            target
          }
        }
      }
    }
  }
}
