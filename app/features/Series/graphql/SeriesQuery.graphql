query Series($id: ID!) {
  series(id: $id) {
    id
    name
    overview
    imdbUrl
    tmdbUrl
    inWatchLater
    poster {
      source
    }
    seasons {
      id
      number
      status
      overview
      imdbUrl
      tmdbUrl
    }
    subscription {
      id
      subscribedOn
      settings {
        spoilerAvoidance
      }
    }
    watchThroughs {
      id
      status
      from {
        season
        episode
      }
      to {
        season
        episode
      }
    }
  }
  clipsInstallations(
    extensionPoint: "Series.Details.RenderAccessory"
    conditions: [{series: {id: $id}}]
  ) {
    id
    extensionPoint
    configuration
    version {
      id
      apiVersion
      assets {
        source
      }
    }
    extension {
      id
      name
    }
  }
}
