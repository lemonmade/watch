query SubscriptionDetails($id: ID!) {
  subscription(id: $id) {
    id
    subscribedOn
    series {
      id
      name
      poster {
        source
      }
    }
    settings {
      spoilerAvoidance
    }
  }
}
