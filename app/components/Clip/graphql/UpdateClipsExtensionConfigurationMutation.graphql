mutation UpdateClipsExtensionConfiguration($id: ID!, $configuration: JSON) {
  updateClipsExtensionInstallation(id: $id, configuration: $configuration) {
    installation {
      id
      configuration
    }
  }
}
