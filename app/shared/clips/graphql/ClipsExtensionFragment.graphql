fragment ClipsExtension on ClipsExtensionInstallation {
  id
  target
  settings
  translations
  liveQuery
  loading {
    ui {
      html
    }
  }
  version {
    id
    apiVersion
    assets {
      source
    }
  }
  extension {
    id
    name
    app {
      id
      name
    }
  }
}
