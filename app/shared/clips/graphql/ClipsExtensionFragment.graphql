fragment ClipsExtension on ClipsExtensionInstallation {
  id
  target
  settings
  liveQuery
  version {
    id
    apiVersion
    assets {
      source
    }
  }
  extension {
    id
    name
    app {
      id
      name
    }
  }
}
