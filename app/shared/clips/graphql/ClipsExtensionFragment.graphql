fragment ClipsExtension on ClipsExtensionInstallation {
  id
  target
  settings
  liveQuery
  loading {
    ui {
      tree
    }
  }
  version {
    id
    apiVersion
    assets {
      source
    }
  }
  extension {
    id
    name
    app {
      id
      name
    }
  }
}
