query LocalDevelopmentOrchestrator {
  app {
    name
    extensions {
      ... on ClipsExtension {
        id
        name
        supports {
          name
          module
          conditions {
            series {
              handle
            }
          }
        }
      }
    }
  }
}
