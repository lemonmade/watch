query LocalDevelopmentOrchestrator {
  app {
    name
    extensions {
      ... on ClipsExtension {
        id
        name
        extends {
          target
          module
          conditions {
            series {
              handle
            }
          }
        }
      }
    }
  }
}
