query FindAppMatchingLocalDevelopment($id: ID, $name: String) {
  me {
    app(id: $id, name: $name) {
      extensions {
        ... on ClipsExtension {
          id
          name
        }
      }
    }
  }
}
