mutation PublishLatestClipsExtensionVersion($id: ID!) {
  publishLatestClipsExtensionVersion(id: $id) {
    version {
      id
      extends {
        target
        entry {
          asHTML {
            content
          }
          asJavaScript {
            src
          }
        }
      }
    }
  }
}
