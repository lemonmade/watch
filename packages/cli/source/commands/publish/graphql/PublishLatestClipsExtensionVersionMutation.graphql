mutation PublishLatestClipsExtensionVersion($extensionId: ID!) {
  publishLatestClipsExtensionVersion(extensionId: $extensionId) {
    version {
      id
      assets {
        source
      }
    }
  }
}
