mutation CreateAppFromLocalApp($name: String!, $handle: String!) {
  createApp(name: $name, handle: $handle) {
    app {
      id
      name
      handle
      extensions {
        __typename
        ... on ClipsExtension {
          id
          name
          handle
        }
      }
    }
  }
}
