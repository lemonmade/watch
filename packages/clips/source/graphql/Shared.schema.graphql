scalar Date
scalar ApiVersion
scalar FormattedText

type User {
  id: ID!
}

type Query {
  user: User!
  version: ApiVersion!
}

type Series {
  id: ID!
  name: String!
}

type WatchThrough {
  id: ID!
  series: Series!
}
